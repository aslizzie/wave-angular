export interface MovieModel {
    _id: string;
    name: string;
    description: string;
    duration: number;
    cover_carrousel: string;
    cover_card: string;
    url: string;
    classification: string;
    favorite: boolean;
}